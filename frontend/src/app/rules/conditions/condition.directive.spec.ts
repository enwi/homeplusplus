import { ConditionDirective } from './condition.directive';

describe('ConditionDirective', () => {
  it('should create an instance', () => {
    const directive = new ConditionDirective();
    expect(directive).toBeTruthy();
  });
});
