import { SubActionDirective } from './sub-action.directive';

describe('SubActionDirective', () => {
  it('should create an instance', () => {
    const directive = new SubActionDirective();
    expect(directive).toBeTruthy();
  });
});
