import { TypeFormDirective } from './type-form.directive';

describe('TypeFormDirective', () => {
  it('should create an instance', () => {
    const directive = new TypeFormDirective();
    expect(directive).toBeTruthy();
  });
});
