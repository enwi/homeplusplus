import { DeviceDetailDirective } from './device-detail.directive';

describe('DeviceDetailDirective', () => {
  it('should create an instance', () => {
    const directive = new DeviceDetailDirective();
    expect(directive).toBeTruthy();
  });
});
